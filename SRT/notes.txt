SRT doesn't support tags, so the WebVTT docs would need to transformed
SRT also doesn't have a formal spec (apparently), so overlaps of

0.
00:00:00.000 --> DURATION
document-wide description

1.
00:00:00.000 --> SECOND SEGMENT
[segment blocks or descriptions]